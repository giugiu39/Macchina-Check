PORT=4000
JWT_SECRET=change_me_secret

# Frontend URL per link reset password
FRONTEND_URL=http://localhost:5173

# Configurazione SMTP (opzionale):
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Promemoria Auto <no-reply@example.com>"